(ns nido.ui.discovery
  "Discovers artifacts from registered projects. VSDD and log helpers removed
   in Task 7 — the /:project/... routes that consumed them were deleted and
   project-context became unreferenced. Retained as a namespace stub to avoid
   surprises if future UI work wants a place to add discovery helpers.")
