/.bb
/.calva
/.clj-kondo
/.lsp
/.DS_Store
/.claude/*.lock
/.nrepl-port

# superpowers brainstorm scratch (visual companion, plans/specs handled separately)
.superpowers/

# nido writes this into every session worktree (nido.session.agent-guidance)
/AGENTS.override.md
